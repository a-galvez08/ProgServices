import { CrearUsuarioModel } from './crear-usuario-model.model';

describe('CrearUsuarioModel', () => {
  it('should create an instance', () => {
    expect(new CrearUsuarioModel()).toBeTruthy();
  });
});
